<!DOCTYPE html>
<html>
<head>
	<title>Agent Contacts</title>
	<link rel="shortcut icon" href="favicon.svg" type="image/svg+xml">

	<style>
		body {
			background-color: #f2f2f2;
			font-family: Arial, sans-serif;
			color: #333;
			padding: 20px;
		}
		h1 {
			font-size: 2rem;
			margin-bottom: 30px;
			text-align: center;
			text-transform: uppercase;
			letter-spacing: 2px;
			color: #ff5a3d;
		}
		table {
			border-collapse: collapse;
			width: 100%;
			margin-top: 30px;
			box-shadow: 0 5px 10px rgba(0,0,0,0.1);
			background-color: #fff;
			border-radius: 5px;
			overflow: hidden;
		}
		th, td {
			padding: 12px;
			text-align: left;
			border-bottom: 1px solid #ddd;
		}
		th {
			background-color: #ff5a3d;
			color: white;
			text-transform: uppercase;
			letter-spacing: 1px;
		}
		tr:hover {
			background-color: #f5f5f5;
			cursor: pointer;
		}
	</style>
</head>
<body>
	<h1>Agent Contacts</h1>
	<table>
		<thead>
			<tr>
				<th>Name</th>
				<th>Email</th>
				<th>Phone</th>
			</tr>
		</thead>
		<tbody>
			<% for (var i = 0; i < agents.length; i++) { %>
				<tr onclick="window.location.href='mailto:<%= agents[i].email %>';">
					<td><%= agents[i].name%></td>
					<td><%= agents[i].email %></td>
					<td><%= agents[i].phone %></td>
				</tr>
			<% } %>
		</tbody>
	</table>
</body>
</html>
